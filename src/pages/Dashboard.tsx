import React from 'react'
import { DashboardData, TrafficSource, RevenueTrend } from '../types/dashboard.types'

const Dashboard: React.FC = () => {
	return <div>Dashboard</div>
}

export default Dashboard
